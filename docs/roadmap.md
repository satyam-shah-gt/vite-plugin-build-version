# Roadmap

Future improvements after the Vite MVP:

- Rollup plugin wrapper.
- Webpack plugin.
- Next.js support.
- Git tag metadata.
- Release channel helpers.
- Multiple app/version files in monorepos.
- Changelog metadata.
- Deployment provider examples.
- Service worker coordination docs.

The MVP should stay focused on generating one JSON file during Vite builds.
